import { Context } from "@entity-core/context";
import { Entity } from "../interfaces";
declare function updateEntity({ context, entity, }: {
    context: Context;
    entity: Entity;
}): Promise<void>;
export default updateEntity;
