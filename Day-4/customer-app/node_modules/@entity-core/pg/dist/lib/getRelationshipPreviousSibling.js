"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const sql_template_strings_1 = __importDefault(require("sql-template-strings"));
function getRelationshipPreviousSibling({ context, relationship, fromID, fromType, entityID, entityType, _lock = false, }) {
    return __awaiter(this, void 0, void 0, function* () {
        const dataSource = context.dataSource;
        const client = (yield context.getDB());
        const entityRelTable = dataSource.tablePrefix + `relationship`;
        const tenantID = context.getTenantID();
        const optionalUpdate = _lock ? `FOR UPDATE` : ``;
        const result = yield client.query(sql_template_strings_1.default `
        SELECT previous, previous_type
          FROM "`
            .append(entityRelTable)
            .append(sql_template_strings_1.default `"
         WHERE tenant_id = ${tenantID}
           AND name = ${relationship}
           AND from_id = ${fromID}
           AND from_type = ${fromType}
           AND to_id = ${entityID}
           AND to_type = ${entityType}
         LIMIT 1
         `)
            .append(optionalUpdate));
        if (result.rows.length > 0) {
            return {
                id: result.rows[0].previous,
                type: result.rows[0].previous_type,
            };
        }
        else {
            return null;
        }
    });
}
exports.default = getRelationshipPreviousSibling;
//# sourceMappingURL=getRelationshipPreviousSibling.js.map