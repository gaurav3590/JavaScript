import { Context } from "@entity-core/context";
import { Entity } from "../interfaces";
declare function getEntities<E extends Entity>({ context, entities, limit, }: {
    context: Context;
    entities: {
        id: string;
        type: string;
    }[];
    limit?: number | null;
}): Promise<E[]>;
export default getEntities;
