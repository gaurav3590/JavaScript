import { Context } from "@entity-core/context";
import { EntityID, EntityType } from "../interfaces";
declare function countChildren({ context, parentID, childEntityType, }: {
    context: Context;
    parentID: EntityID;
    childEntityType: EntityType;
}): Promise<number>;
export default countChildren;
