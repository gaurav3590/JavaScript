import { Context } from "@entity-core/context";
import { EntityID, EntityType, EntityPlacement, EntitySiblings } from "../interfaces";
declare function getSiblings({ context, childEntityID, childEntityType, placement, _lock, }: {
    context: Context;
    childEntityID?: EntityID;
    childEntityType: EntityType;
    placement: EntityPlacement | null;
    _lock: boolean;
}): Promise<EntitySiblings>;
export default getSiblings;
