import { Context } from "@entity-core/context";
import { EntityUuid, EntityType, Entity } from "../interfaces";
declare function getEntityByUuid<E extends Entity>({ context, uuid, type, _lock, }: {
    context: Context;
    uuid: EntityUuid;
    type: EntityType;
    _lock?: boolean;
}): Promise<E | null>;
export default getEntityByUuid;
