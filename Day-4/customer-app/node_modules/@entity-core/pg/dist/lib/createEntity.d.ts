import { Context } from "@entity-core/context";
import { Entity, EntityPlacement } from "../interfaces";
declare function createEntity<E extends Entity>({ context, entity, placement, }: {
    context: Context;
    entity: Entity;
    placement?: EntityPlacement;
}): Promise<E>;
export default createEntity;
