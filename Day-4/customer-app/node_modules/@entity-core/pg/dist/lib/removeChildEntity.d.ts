import { Context } from "@entity-core/context";
import { EntityID, EntityType } from "../interfaces";
declare function removeChildEntity({ context, id, type, }: {
    context: Context;
    id: EntityID;
    type: EntityType;
}): Promise<void>;
export default removeChildEntity;
