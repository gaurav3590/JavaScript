"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const sql_template_strings_1 = __importDefault(require("sql-template-strings"));
function getLastChildID({ context, parentID, parentType, childEntityType, _lock = false, }) {
    return __awaiter(this, void 0, void 0, function* () {
        const dataSource = context.dataSource;
        const client = (yield context.getDB());
        const tenantID = context.getTenantID();
        const table = dataSource.tablePrefix + `entity`;
        const optionalUpdate = _lock ? `FOR UPDATE` : ``;
        const { rows } = yield client.query(sql_template_strings_1.default `
            SELECT e.id FROM "`
            .append(table)
            .append(sql_template_strings_1.default `" e
                    WHERE e.tenant_id = ${tenantID}
                    AND e. entity_type = ${childEntityType}
                    AND e. parent = ${parentID}
                    AND e. parent_type = ${parentType}
                    -- AND there is no next row referencing this (so it's the last)
                    AND NOT EXISTS (
                        SELECT
                        FROM "`
            .append(table)
            .append(sql_template_strings_1.default `" n
                        WHERE n.tenant_id = ${tenantID}
                        AND n.entity_type = ${childEntityType}
                        AND n.parent = ${parentID}
                        AND n.parent_type = ${parentType}
                        AND n.previous = e.id
                        LIMIT 1
                      )
                    LIMIT 1
                `))
            .append(optionalUpdate));
        if (rows.length > 0) {
            return rows[0].id;
        }
        return null;
    });
}
exports.default = getLastChildID;
//# sourceMappingURL=getLastChildID.js.map