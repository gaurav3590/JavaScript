import { Context } from "@entity-core/context";
import { EntityID, EntityType } from "../interfaces";
declare function getNextSiblingID({ context, id, type, _lock, }: {
    context: Context;
    id: EntityID;
    type: EntityType;
    _lock: boolean;
}): Promise<EntityID>;
export default getNextSiblingID;
