import { Context } from "@entity-core/context";
import { EntityID, EntityType, Entity } from "../interfaces";
declare function getEntity<E extends Entity>({ context, id, type, _lock, }: {
    context: Context;
    id: EntityID;
    type: EntityType;
    _lock?: boolean;
}): Promise<E | null>;
export default getEntity;
