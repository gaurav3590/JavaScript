import { Context } from "@entity-core/context";
import { EntityID, EntityType, EntityParent } from "../interfaces";
declare function getEntityParent({ context, id, type, }: {
    context: Context;
    id: EntityID;
    type: EntityType;
}): Promise<EntityParent | null>;
export default getEntityParent;
