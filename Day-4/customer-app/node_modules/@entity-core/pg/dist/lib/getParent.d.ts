import { Context } from "@entity-core/context";
import { EntityID, EntityType, Entity } from "../interfaces";
declare function getParent<E extends Entity>({ context, id, type, }: {
    context: Context;
    id: EntityID;
    type: EntityType;
}): Promise<E | null>;
export default getParent;
