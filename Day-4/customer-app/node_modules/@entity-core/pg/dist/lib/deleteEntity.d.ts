import { Context } from "@entity-core/context";
import { EntityID, EntityType } from "../interfaces";
declare function deleteEntity({ context, id, type, }: {
    context: Context;
    id: EntityID;
    type: EntityType;
}): Promise<void>;
export default deleteEntity;
