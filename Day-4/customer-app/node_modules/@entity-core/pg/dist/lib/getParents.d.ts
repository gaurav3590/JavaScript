import { Context } from "@entity-core/context";
import { EntityID, EntityType, EntityParent } from "../interfaces";
declare function getParents({ context, id, type, limit, }: {
    context: Context;
    id: EntityID;
    type: EntityType;
    limit?: number;
}): Promise<EntityParent[]>;
export default getParents;
