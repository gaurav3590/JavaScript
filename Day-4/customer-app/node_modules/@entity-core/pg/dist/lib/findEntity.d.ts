import { Context } from "@entity-core/context";
import { Entity, EntityType } from "../interfaces";
declare function findEntity<E extends Entity>({ context, props, type, }: {
    context: Context;
    props: Record<string, string | number | null | boolean>;
    type: EntityType;
}): Promise<E | null>;
export default findEntity;
