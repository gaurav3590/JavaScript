import { Context } from "@entity-core/context";
import { EntityID, EntityType, Entity } from "../interfaces";
declare function getChildren<E extends Entity>({ context, parentID, parentType, childType, fromID, limit, }: {
    context: Context;
    parentID: EntityID;
    parentType: EntityType;
    childType: EntityType;
    fromID?: EntityID | null;
    limit?: number;
}): Promise<E[]>;
export default getChildren;
