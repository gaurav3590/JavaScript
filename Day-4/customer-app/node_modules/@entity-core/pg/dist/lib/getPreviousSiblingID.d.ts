import { Context } from "@entity-core/context";
import { EntityID, EntityType } from "../interfaces";
declare function getPreviousSiblingID({ context, id, type, _lock, }: {
    context: Context;
    id: EntityID;
    type: EntityType;
    _lock: boolean;
}): Promise<EntityID | null>;
export default getPreviousSiblingID;
