import { PoolConfig } from "pg";
import { DataSource, Client } from "@entity-core/datasource";
import PostgresClient from "./PostgresClient";
declare class PostgresDataSource extends DataSource {
    private poolConfig;
    private pool;
    tablePrefix: string;
    private clients;
    constructor({ poolConfig, tablePrefix, }: {
        poolConfig: PoolConfig;
        tablePrefix?: string;
    });
    private createPool;
    connect(): Promise<void>;
    disconnect(): Promise<void>;
    getClient(): Promise<PostgresClient>;
    registerClient(client: Client): void;
    deregisterClient(client: Client): void;
}
export default PostgresDataSource;
