import { PoolClient, QueryResult } from "pg";
import { Client } from "@entity-core/datasource";
import { DataSource } from "@entity-core/datasource";
import { SQLStatement } from "sql-template-strings";
declare class PostgresClient extends Client {
    dataSource: DataSource;
    poolClient: PoolClient;
    constructor(poolClient: PoolClient, dataSource: DataSource);
    query<R>(query: string | SQLStatement, variables?: Array<unknown>): Promise<QueryResult<R>>;
    release(): Promise<void>;
    on(event: string, callback: (query: string, variables: Array<unknown>) => unknown): Promise<void>;
}
export default PostgresClient;
