import { DataSource, Client } from "@entity-core/datasource";
declare class Context {
    dataSource: DataSource;
    dbClient: Client;
    tenantID: number;
    uuidGenerator: () => string;
    constructor({ dataSource, tenantID, uuidGenerator, }: {
        dbClient?: Client;
        dataSource: DataSource;
        tenantID?: number;
        uuidGenerator?: () => string;
    });
    getTenantID(): number;
    setTenantID(tenantID: number): void;
    uuid(): string;
    getDB(): Promise<Client>;
    end(): Promise<void>;
}
export default Context;
