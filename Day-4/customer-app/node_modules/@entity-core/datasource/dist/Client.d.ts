declare abstract class Client {
    abstract query(query: string, values?: Array<unknown>): Promise<unknown>;
    abstract release(): Promise<void>;
    abstract on(event: string, callback: (query: string, variables: Array<unknown>) => unknown): Promise<void>;
}
export default Client;
